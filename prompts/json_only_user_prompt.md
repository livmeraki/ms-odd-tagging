Evaluate this driving window.

Required taxonomy labels:

{{TAXONOMY_JSON}}

Window metadata:

{
  "recording_id": "{{RECORDING_ID}}",
  "window_id": "{{WINDOW_ID}}",
  "model_mode": "{{MODE}}"
}

Input JSON:

{{REFINED_JSON}}