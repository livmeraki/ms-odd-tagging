Evaluate this driving frame.

Required taxonomy labels:

{{TAXONOMY_JSON}}

Frame metadata:

{
  "recording_id": "{{RECORDING_ID}}",
  "frame_id": "{{FRAME_ID}}",
  "model_mode": "{{MODE}}"
}

Input JSON:

{{REFINED_JSON}}
